/// A Math module
pub mod math {

  /// Sum two numbers
  ///
  /// ```
  /// let result = hello::common::math::sum(2, 3);
  /// assert_eq!(result, 5);
  /// ```
  pub fn sum(x: i32, y: i32) -> i32 {
    x + y
  }
}